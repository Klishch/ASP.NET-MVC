@model IEnumerable<ObuvkaStore.Models.EntityModels.Seasons>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.season_ua)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.season_ru)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.season_eng)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.season_ua)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.season_ru)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.season_eng)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
